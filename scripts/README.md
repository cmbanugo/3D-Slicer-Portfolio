# Automation Scripts

## volume_calculator.py
Automates volume calculation for all segments in a segmentation using the SegmentStatistics API.

### Usage
1. Open your segmentation in 3D Slicer
2. Open Python Interactor (`View` → `Python Interactor`)
3. Run the script

### Output
Prints volume (mm³ and cm³) for each segment to the console.
